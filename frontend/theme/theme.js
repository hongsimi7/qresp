import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#800000",
    },
    secondary: {
      main: "#777777",
    },
    background: {
      default: "#FFF",
    },
  },
});

export default Theme;
