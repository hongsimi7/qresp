# Qresp backend dependencies.
#
# 2026-07-02 audit (see DEPENDENCY_AUDIT.md): removed every declared-but-never-
# imported package (Flask-API, Flask-HTTPAuth, flask-profiler, Flask-WTF,
# paramiko, schedule, py3dns, pyasn1, validate-email, pyOpenSSL,
# swagger-spec-validator, coveralls) and redundant explicit transitives
# (itsdangerous, Jinja2, urllib3). Added `requests` explicitly (used by
# project/util.py; was previously satisfied only transitively).
#
# Remaining caps, being lifted in staged phases (FULL_STACK_MODERNIZATION_REPORT.md):
#   * Flask<2.3 / Werkzeug<2.3 -- Connexion 2.14 requires flask<2.3, and
#     flask-mongoengine 1.0 breaks on Flask>=2.3 (removed flask.json APIs).
#   * connexion[swagger-ui]<3.0 -- Connexion 3 is an ASGI rewrite (migration
#     staged next).
# Exact verified versions are captured in requirements.lock.txt.
setuptools
jsonschema
Flask<2.3
Werkzeug<2.3
Flask-Cors
flask-mongoengine
Flask-Session
flask-sitemap
mongoengine
pymongo
requests
requests_oauthlib
WTForms
lxml
gunicorn
connexion[swagger-ui]<3.0
# --- dev / test tools ---
pre-commit
mongomock
coverage
nose2
