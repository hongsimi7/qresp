# Qresp backend dependencies.
#
# Modernized where verifiable: WTForms 3.x, MongoEngine 0.29.x, PyMongo 4.x
# (code updated in project/views.py and project/tests/test_paperDAO.py).
#
# Two caps REMAIN, and are hard blockers documented in
# FULL_STACK_MODERNIZATION_REPORT.md (require a large migration, not done here):
#   * Flask<2.3 / Werkzeug<2.3 -- Connexion 2.14 requires flask<2.3, and
#     flask-mongoengine 1.0 breaks on Flask>=2.3 (removed flask.json APIs).
#   * connexion[swagger-ui]<3.0 -- Connexion 3 is an ASGI rewrite; the app uses
#     connexion.FlaskApp + `from connexion import jsonifier` (removed in 3.0).
# Exact verified versions are captured in requirements.lock.txt.
setuptools
jsonschema
Flask<2.3
Werkzeug<2.3
Flask-API
Flask-Cors
Flask-HTTPAuth
flask-mongoengine
flask-profiler
Flask-Session
Flask-WTF
itsdangerous
Jinja2
mongoengine
paramiko
pre-commit
py3dns
pyasn1
pymongo
pyOpenSSL
swagger-spec-validator
urllib3
validate-email
WTForms
schedule
flask-sitemap
requests_oauthlib
mongomock
connexion[swagger-ui]<3.0
coverage
nose2
coveralls
lxml
gunicorn
